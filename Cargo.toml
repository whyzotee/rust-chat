[package]
name = "rust-chat"
version = "0.1.0"
edition = "2024"

[dependencies]
# Core
axum = { version = "0.8.4", features = ["ws"] }
dotenv = "0.15.0"
# Json Serialization
serde_json = "1.0"
# Asynchronous runtime
tokio = { version = "1", features = ["full"] }
